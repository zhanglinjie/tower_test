bootbox.hideAll();
$(".<%= singular_name%>-tr[data-id='<%%= @<%= singular_name%>.id%>']").replaceWith("<%%= j render partial: 'table_tr', locals: { <%= singular_name%>: @<%= singular_name%>}%>");
var item = $(".<%= singular_name%>-tr[data-id='<%%= @<%= singular_name%>.id%>']");
item.addClass("update-active");
setTimeout(function(){
  item.removeClass("update-active");
}, 300);