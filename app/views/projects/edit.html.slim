.header
  h2 编辑#{tm('project')}

= render partial: 'updation_form', locals: { remote: false }

=> link_to t('Show'), [current_team, @project], :class=>"btn btn-sm btn-default"
=> link_to t('Back'), [current_team, :projects], :class=>"btn btn-sm btn-default"
