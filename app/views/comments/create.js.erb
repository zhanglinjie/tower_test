bootbox.hideAll();
$(".comments-table > table > tbody > tr.no-record-tr").remove();
var item = $("<%= j render partial: 'table_tr', locals: { comment: @comment}%>");
$(".comments-table > table > tbody").append(item);
item.addClass("update-active");
setTimeout(function(){
  item.removeClass("update-active");
}, 300);